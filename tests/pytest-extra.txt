# Python suites that live OUTSIDE tests/ and are gated anyway, one path per line.
#
# A skill's tests belong beside the skill; that does not make them optional. These run in
# plugin-audit's pytest leg alongside tests/.
#
# NOT listed, and why — each would need a credential or a live service, so gating them would make
# the audit fail on a laptop with no VPN rather than on a defect:
#   skills/wrds/scripts/test_connection.py         WRDS PostgreSQL
#   skills/lseg-data/scripts/test_connection.py    LSEG Data Library
#   skills/gemini-batch/scripts/test_single.py     Gemini API
#
# workshop_deck_test.py IS listed. It was documented for months as failing "19 of 108 on a clean
# tree in the WID and INV legs" — a plan scoped work around that number and told implementers it
# was not theirs to fix. It was one undeclared dependency: with `pypdf` on the path all 108 pass.
# The failures said so ("`pypdf` is unimportable, so no page text could be extracted") to a suite
# nothing ran.
scripts/cite-fidelity/test_common.py
skills/pincite/tests/test_apply.py
skills/pincite/tests/test_review_serve.py
skills/writing/scripts/writing_bench_compile_test.py
skills/writing/scripts/writing_section_index_test.py
skills/wrds/scripts/edgar_parquet/test_edgar_parquet.py
skills/workshop/scripts/workshop_deck_test.py
